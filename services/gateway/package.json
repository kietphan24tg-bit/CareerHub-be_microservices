{
  "name": "@careerhub/gateway",
  "version": "0.1.0",
  "private": true,
  "main": "src/main.ts",
  "types": "src/main.ts",
  "dependencies": {
    "@careerhub/contracts": "workspace:*",
    "@careerhub/infrastructure": "workspace:*",
    "@careerhub/nest-common": "workspace:*",
    "@careerhub/shared-kernel": "workspace:*",
    "@grpc/grpc-js": "^1.14.4",
    "@grpc/proto-loader": "^0.8.1",
    "@nestjs/common": "^11.1.19",
    "@nestjs/config": "^4.0.2",
    "@nestjs/core": "^11.1.24",
    "@nestjs/platform-express": "^11.1.24",
    "@opentelemetry/api": "^1.9.1",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "reflect-metadata": "^0.2.2",
    "playwright": "^1.55.0",
    "rxjs": "^7.8.2"
  },
  "scripts": {
    "build": "pnpm -C ../.. build:packages && tsc -p tsconfig.json",
    "dev": "pnpm run build && node dist/services/gateway/src/main.js",
    "start": "node dist/services/gateway/src/main.js",
    "test:e2e:candidate-module:live": "powershell -Command \"if (Test-Path dist-test) { Remove-Item -Recurse -Force dist-test }; & ..\\..\\node_modules\\.bin\\tsc.cmd -p tsconfig.spec.json; $files = Get-ChildItem -Recurse -File dist-test | Where-Object { $_.Name -eq 'candidate-module.integration.live.e2e.spec.js' } | ForEach-Object { $_.FullName }; node --test $files\"",
    "test:e2e:job-phase:live": "powershell -Command \"if (Test-Path dist-test) { Remove-Item -Recurse -Force dist-test }; & ..\\..\\node_modules\\.bin\\tsc.cmd -p tsconfig.spec.json; $files = Get-ChildItem -Recurse -File dist-test | Where-Object { $_.Name -eq 'job-phase.integration.live.e2e.spec.js' } | ForEach-Object { $_.FullName }; node --test $files\"",
    "test:e2e:application-phase:live": "powershell -Command \"if (Test-Path dist-test) { Remove-Item -Recurse -Force dist-test }; & ..\\..\\node_modules\\.bin\\tsc.cmd -p tsconfig.spec.json; $files = Get-ChildItem -Recurse -File dist-test | Where-Object { $_.Name -eq 'application-phase.integration.live.e2e.spec.js' } | ForEach-Object { $_.FullName }; node --test $files\"",
    "test:e2e:interview-offer-phase:live": "powershell -Command \"if (Test-Path dist-test) { Remove-Item -Recurse -Force dist-test }; & ..\\..\\node_modules\\.bin\\tsc.cmd -p tsconfig.spec.json; $files = Get-ChildItem -Recurse -File dist-test | Where-Object { $_.Name -eq 'interview-offer-phase.integration.live.e2e.spec.js' } | ForEach-Object { $_.FullName }; node --test $files\"",
    "test:e2e:gap-closure-phase:live": "powershell -Command \"if (Test-Path dist-test) { Remove-Item -Recurse -Force dist-test }; & ..\\..\\node_modules\\.bin\\tsc.cmd -p tsconfig.spec.json; $files = Get-ChildItem -Recurse -File dist-test | Where-Object { $_.Name -eq 'gap-closure-phase.integration.live.e2e.spec.js' } | ForEach-Object { $_.FullName }; node --test $files\"",
    "test:e2e:resume-export-pdf:live": "powershell -Command \"if (Test-Path dist-test) { Remove-Item -Recurse -Force dist-test }; & ..\\..\\node_modules\\.bin\\tsc.cmd -p tsconfig.spec.json; $files = Get-ChildItem -Recurse -File dist-test | Where-Object { $_.Name -eq 'resume-export-pdf.integration.live.e2e.spec.js' } | ForEach-Object { $_.FullName }; node --test $files\"",
    "test:e2e:register-candidate:live": "powershell -Command \"if (Test-Path dist-test) { Remove-Item -Recurse -Force dist-test }; & ..\\..\\node_modules\\.bin\\tsc.cmd -p tsconfig.spec.json; $files = Get-ChildItem -Recurse -File dist-test | Where-Object { $_.Name -eq 'register-candidate.integration.live.e2e.spec.js' } | ForEach-Object { $_.FullName }; node --test $files\"",
    "test:e2e:password-reset:live": "powershell -Command \"if (Test-Path dist-test) { Remove-Item -Recurse -Force dist-test }; & ..\\..\\node_modules\\.bin\\tsc.cmd -p tsconfig.spec.json; $files = Get-ChildItem -Recurse -File dist-test | Where-Object { $_.Name -eq 'password-reset.integration.live.e2e.spec.js' } | ForEach-Object { $_.FullName }; node --test $files\"",
    "test:e2e:live": "powershell -Command \"if (Test-Path dist-test) { Remove-Item -Recurse -Force dist-test }; & ..\\..\\node_modules\\.bin\\tsc.cmd -p tsconfig.spec.json; $files = Get-ChildItem -Recurse -File dist-test | Where-Object { $_.Name -like '*.live.e2e.spec.js' } | ForEach-Object { $_.FullName }; node --test $files\"",
    "test": "powershell -Command \"if (Test-Path dist-test) { Remove-Item -Recurse -Force dist-test }; & ..\\..\\node_modules\\.bin\\tsc.cmd -p tsconfig.spec.json; $files = Get-ChildItem -Recurse -File dist-test | Where-Object { $_.Name -like '*.spec.js' -and $_.Name -notlike '*.live.e2e.spec.js' } | ForEach-Object { $_.FullName }; node --test --test-concurrency=1 $files\""
  }
}
