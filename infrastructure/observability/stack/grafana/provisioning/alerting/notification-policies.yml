apiVersion: 1

policies:
  - orgId: 1
    receiver: CareerHub Email
    group_by:
      - alertname
      - service
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    matchers:
      - alertname =~ "CareerHub.*"
